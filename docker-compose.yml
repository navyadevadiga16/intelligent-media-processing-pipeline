version: '3.9'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/usr/src/app/uploads
      - ./data.db:/usr/src/app/data.db
    environment:
      - PORT=3000
